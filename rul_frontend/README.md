# RUL Prediction - Streamlit Frontend

This is the frontend UI for the Predictive Maintenance RUL Prediction System.

### 🚀 How it works
- Upload a CSV with 38 sensor features.
- It sends data to the backend API on Railway.
- Displays predicted RUL + status level.

### Backend API
Be sure to update the `API_URL` inside `streamlit_app.py`.

### Deployment
Deploy this repo on Railway as a **Static Web Service (Streamlit)**.

