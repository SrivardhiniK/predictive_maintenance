name: rul-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - tensorflow==2.12.0
      - flask
      - flask-cors
      - numpy
      - pandas
      - scikit-learn
